/* 해당 파일은 함수들의 parameter, result를 정의하며
 * 접두사를 붙이는 형태로 타입을 구분한다
 * PR - parameter | type
 * RT - result
 * */

/**
 * BOX - 박스
 * CIRCLE - 원형
 * */
export const enum PR_SHARE_RENDER_HELPER {
  BOX = "box",
  CIRCLE = "circle",
}
